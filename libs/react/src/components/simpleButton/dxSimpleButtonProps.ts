import {ISimpleButtonState} from '@dx/core/types/simpleButton';
import {TReactInputs} from '../../common/types';

interface IDxSimpleButtonProps extends TReactInputs<ISimpleButtonState> {}

export type {
  IDxSimpleButtonProps,
}
